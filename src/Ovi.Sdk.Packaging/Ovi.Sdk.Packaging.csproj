<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>The .ovipkg package format for the Ovi workflow SDK: a zip container with a manifest describing the nodes (agents, tools, triggers) it carries. This project covers authoring and reading packages; loading them into a runtime comes later.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Ovi.Sdk.Nodes/Ovi.Sdk.Nodes.csproj" />
  </ItemGroup>

</Project>
